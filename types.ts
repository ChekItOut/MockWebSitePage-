
export interface SiteTheme {
  headline: string;
  subheadline: string;
  primaryColor: string;
  secondaryColor: string;
  vibe: string;
}

export interface FeatureItem {
  title: string;
  description: string;
  icon: string;
}
